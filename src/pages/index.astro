---
import Layout from "../components/Layout.astro";
---

<Layout>
	<div class="container">
		<div class="flex flex-col-reverse lg:flex-row gap-4 p-4 bg-base-200 rounded-lg">
			<div class="flex flex-col justify-center items-start flex-1">
				<div class="badge">Electronics</div>
				<h2 class="text-5xl font-bold mt-4">Life Advice Looking Through A Window</h2>
				<p class="text-lg opacity-80 mt-2">it won't be a bigger problem to find one video game lover in your neighbor</p>
				<button class="btn btn-primary btn-lg mt-4">Buy now</button>
			</div>
			<div>
				<img width="566" height="368" class="hidden mx-auto w-full sm:block object-cover rounded-lg" src="https://fakeimg.pl/566x368/" />
			</div>
		</div>
		<div class="space-y-8 my-12">
			<div>
				<h3 class="text-3xl font-bold">Popular Caterogies</h3>
				<div class="flex flex-wrap gap-4 mt-4">
					{
						Array(4)
							.fill(0)
							.map(() => {
								return <div class="w-full sm:w-[280px] h-[200px] bg-base-200" />;
							})
					}
				</div>
			</div>
			<div>
				<h3 class="text-3xl font-bold">Popular Caterogies</h3>
				<div class="flex flex-wrap gap-4 mt-4">
					{
						Array(4)
							.fill(0)
							.map(() => {
								return <div class="w-full sm:w-[280px] h-[200px] bg-base-200" />;
							})
					}
				</div>
			</div>
		</div>
	</div>
</Layout>
